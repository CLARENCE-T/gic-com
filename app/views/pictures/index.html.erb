  <a class="prev" >&#10094;</a>
  <a class="next" >&#10095;</a>
<h1 class="events-pics-name"> <%= @event.title %> </h1>
<div id="masonry-container" class="grid">
  <% @event.pictures.each_with_index do |picture, idx| %>
    <div class="grid-item <%= 'grid-item--width2' if (idx % 3 == 0) %>">

        <%= cl_image_tag(picture.image.key, quality: 100 ,alt: picture.description, data: {id: idx} ) %>
        <p><%= picture.description %> </p>

    </div>
  <% end %>
</div>


