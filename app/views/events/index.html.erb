<div class="mosaic_pics">
  <% @events.each do |event| %>
    <%= link_to event_path(event) do %>
      <%= cl_image_tag(event.pictures.first.image.key, alt: event.title, width: 200, height: 200, crop: :thumb, gravity: :face, dpr: 4.0) %>
    <% end %>
  <% end %>
</div>
