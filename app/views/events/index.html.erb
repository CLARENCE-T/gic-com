
<% @events.each do |event| %>
  <%= link_to event_path(event) do %>
    <%= cl_image_tag(event.images.first.key, width: 200, height: 200, crop: :thumb, gravity: :face, dpr: 4.0) %>
  <% end %>
<% end %>
